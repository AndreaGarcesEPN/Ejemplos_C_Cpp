' Este codigo ha sido generado por el modulo psexport 20180802-w32 de PSeInt.
' Es posible que el codigo generado no sea completamente correcto. Si encuentra
' errores por favor reportelos en el foro (http://pseint.sourceforge.net).

Module EJERCICIOVARIABLES

	Sub Main()
		Dim anio As String
		Dim anio2 As String
		Dim fal As String
		Dim numero1 As Integer
		Dim numero2 As String
		Dim numneg As Integer
		Dim numpi As Double
		Dim text As String
		Dim verd As Boolean
		Dim anio3(5) As Integer
		numero1 = 5
		numero2 = "5"
		text = "5"
		numneg = -4
		numpi = 3.14
		anio = "2019"
		anio2 = "2019"
		verd = True
		fal = "Falso"
		anio3(1) = 2
		anio3(2) = 0
		anio3(3) = 1
		anio3(4) = 9
		Console.WriteLine(numero1)
		Console.WriteLine(numero2)
		Console.WriteLine(text)
		Console.WriteLine(numneg)
		Console.WriteLine(numpi)
		Console.WriteLine(anio)
		Console.WriteLine(anio2)
		Console.WriteLine(verd)
		Console.WriteLine(fal)
		Console.WriteLine(anio3(1),anio3(2),anio3(3),anio3(4))
	End Sub

End Module
